{
    "name": "cdk",
    "version": "1.0.0",
    "description": "AWS CDK Infrastructure",
    "scripts": {
        "build": "tsc",
        "deploy": "cdk deploy --app 'npx ts-node bin/my-repo.ts'",
        "destroy": "cdk destroy",
        "cdk-synth": "cdk synth",
        "cdk-deploy": "cdk deploy"

    },
    "dependencies": {
        "aws-cdk-lib": "^2.178.2",
        "constructs": "^10.4.2"
    },
    "devDependencies": {
        "typescript": "^4.9.3"
    }
}
